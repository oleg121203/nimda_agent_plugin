#!/bin/bash
# Simple script to mark Codex session as active
# Call this from Codex chat to reset the timeout

./codex_monitor.sh mark-active

echo "✅ Codex session marked as active"
echo "🔄 Timeout reset - local monitor will wait another minute"
