# NIMDA Agent Plugin Requirements
# Minimal dependencies for the plugin

# Core Python libraries (usually already installed)
pathlib2>=2.3.7; python_version<"3.4"

# HTTP requests for GitHub API and other services
requests>=2.28.0

# Environment variable handling
python-dotenv>=1.0.0

# Working with YAML files (codex.yaml, workflows)
PyYAML>=6.0

# Better JSON utilities
jsonschema>=4.0.0

# Date and time utilities (for compatibility)
python-dateutil>=2.8.0

# Optional dependencies for extended functionality

# Git operations (if system git is unavailable)
# GitPython>=3.1.0

# Advanced logging
# colorlog>=6.0.0

# Progress bars for long operations
# tqdm>=4.64.0

# Configuration file handling
# configparser>=5.0.0  # Built into Python 3.8+

# Markdown validation and parsing
# markdown>=3.4.0

# Async support (for future versions)
# asyncio  # Built into Python 3.8+

# Testing (for plugin development)
# pytest>=7.0.0
# pytest-cov>=4.0.0

# Linting and formatting (for development)
# black>=22.0.0
# flake8>=5.0.0
# mypy>=0.991
